{
  "name": "socket-io-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:server": "nodemon --ignore ./client server.js --ignore ./db/rooms",
    "dev:client": "npm start --prefix ./client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "postinstall": "cd client && npm i"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^3.0.2",
    "socket.io-client": "^3.0.2"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6"
  }
}
